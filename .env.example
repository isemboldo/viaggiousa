# App
APP_ENV=local
APP_DEBUG=true
APP_URL_BASE=/viaggiousa   # base path per rewrite (XAMPP: /viaggiousa, prod: /viaggiousa)

APP_TIMEZONE=Europe/Zurich

# Database (XAMPP - locale)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=bbkd_viaggiousa
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Database (Produzione Infomaniak) — scommenta e compila in prod
# APP_ENV=production
# APP_DEBUG=false
# DB_HOST=bbkd.myd.infomaniak.com
# DB_PORT=3306
# DB_NAME=bbkd_viaggiousa
# DB_USER=XXXXXXXX
# DB_PASS=XXXXXXXX
# DB_CHARSET=utf8mb4
# DB_COLLATION=utf8mb4_unicode_ci

# SMTP (per PHPMailer) — placeholder
SMTP_HOST=smtp.infomaniak.com
SMTP_PORT=465
SMTP_USER=no-reply@isohome.ch
SMTP_PASS=changeme
SMTP_FROM_EMAIL=no-reply@isohome.ch
SMTP_FROM_NAME="Viaggio USA"

# Twig
TWIG_CACHE=false
TWIG_CACHE_PATH=/tmp/twig-cache

# FX caching
FX_CACHE_ENABLED=true
FX_CACHE_TTL=21600
